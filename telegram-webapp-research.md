# Telegram Mini App verification notes

المصدر الرسمي: https://core.telegram.org/bots/webapps

يجب أن يرسل تطبيق Telegram Mini App قيمة `Telegram.WebApp.initData` إلى الخادم، ولا يجوز الوثوق بـ `initDataUnsafe` وحدها. يتحقق الخادم من التوقيع وفق خوارزمية Telegram Web Apps باستخدام رمز البوت، ويفحص حداثة `auth_date` قبل قبول الزيارة. بعد التحقق يمكن ربط `user.id` بمعرّف تيليغرام في جلسة زيارة، ثم تسجيل حالة الزيارة في Supabase. لا يُسجَّل إثبات الزيارة اعتمادًا على مجرد الضغط على رابط خارجي.

التصميم المستهدف: زر Web App من بوت الناصر يفتح صفحة `/levels` داخل Telegram؛ الصفحة ترسل `initData` إلى نقطة تحقق خادمية؛ الخادم يتحقق من التوقيع ثم يكتب سجل زيارة في Supabase مع معرّف تيليغرام ووقت الزيارة ومرجع جلسة أحادي الاستخدام؛ بعدها يعيد حالة نجاح للواجهة، وتصبح البوابة في البوت مستندة إلى إثبات خادمي لا إلى callback يدوي.
