# تصميم مركز إدارة بوت الناصر

## مبدأ التحكم الآمن

يعتمد المركز على فصل **بيانات العرض القابلة للإدارة** عن منطق البوت الحرج. لا تتاح من اللوحة أي صلاحية لتغيير رمز البوت أو الرمز السري للويب هوك أو قواعد الاشتراك الإلزامي أو معالجة الدفعات. تبقى هذه العناصر إعدادات خادمية محمية، بينما يدير المسؤول ظهور الأقسام ومحتوى الرسائل وترتيب الملفات ضمن الصلاحيات المحددة.

| المجال | مصدر البيانات الحالي | أسلوب الإدارة المقترح | أثر التغيير |
|---|---|---|---|
| أزرار إضافية | `telegram_managed_menu_items` | إضافة وتعديل وتعطيل وترتيب | تظهر في القائمة الرئيسية عند فتحها التالي |
| أقسام ثابتة | أزرار مضمّنة في `mainMenu()` | سجل إعدادات لكل قسم: الاسم، الظهور، الترتيب، الوصف | يمنع إظهار القسم المخفي مع بقاء منطق الوصول والاشتراك |
| ملفات ومجلدات | `legal_sources` و`legal_folders` | إخفاء آمن، تعديل الاسم والوصف والترتيب، ورفع ملف جديد إلى S3 | لا يحذف ملفات المصدر ولا يغير Google Drive تلقائيًا |
| رسائل تشغيلية | نصوص البوت الثابتة ومسودات البث | قوالب رسائل محددة تسمح بمسودات ومراجعة قبل النشر | يمنع تغيير رسائل الحماية ويدعم البث بعد تأكيد صريح |
| عمليات المسؤول | `telegram_admin_audit_logs` | سجل غير قابل للتعديل داخل الواجهة | يحفظ نوع العملية والعنصر والوقت وهوية المسؤول من دون أسرار |

## نموذج التنفيذ المرحلي

تُنفذ المرحلة التالية بإضافة سجل أقسام مدارة ومفاتيح معروفة فقط، مثل `browse` و`judicial` و`legislation` و`legal_forms` و`exams` و`secondary_exams`. لا يُقبل مفتاح جديد من الواجهة لتجنب إنشاء مسارات لا يغطيها منطق البوت. وتستعمل البيانات الحالية للمكتبة كمصدر للحقيقة، مع حقول `enabled` و`sortOrder` و`displayLabel` تضبط العرض فحسب.

تُدار الملفات على مستوى الفهرس، وليس بتعديل روابط Google Drive الأصلية. يرفع أي ملف جديد إلى التخزين الكائني ويحفظ مفتاحه ورابطه في سجل مصدر قانوني جديد، بينما يعطل الإخفاء إمكانية الوصول من البوت من دون حذف السجل الأصلي أو الكائن المخزن. وتسبق أي عملية حذف أو بث رسالة شاشة تأكيد صريحة وسجل تدقيق.
